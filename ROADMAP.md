# Baseball Manager 2025 — ロードマップ

> 最終更新: 2026-03-26（**次フォーカス: T3/T4 リファクタリング → Tier 8** — T3（App.jsx分割）・T4（Tabs.jsx分割）を Tier 8 着手前に完了させる方針に決定。T7 Vitest 部分実装済み）
> **ゴール**: NPB版 OOTP / Football Manager — 深いシミュレーションと長期フランチャイズ経営
> **運用ルール**: 実装完了した項目は ✅ に更新。新規項目は末尾に追記。過去の記録は削除しない。

---

## Tier 1（基盤強化）— ✅ 完了

| # | 機能 | 内容 | 実装コミット |
|---|------|------|-------------|
| ⓪ | カレンダーシステム + 在籍年数基盤 | 実際の日付表示・週間スケジュール・オールスター休止・serviceYears/entryAge フィールド追加 | 2503dd5 |
| ① | 投手利き腕 × vsLeft 補正 | 左投手対戦時に vsLeft 能力値で打者の contact・power を補正 | 2503dd5 |
| ② | 投手統計拡充（SV / HLD / QS / BS） | 中継ぎ評価指標の追加・Tabs.jsx 投手成績表への表示 | 2503dd5 |
| ③ | 中継ぎ連投疲労 | recentPitchingDays による連続登板日数追跡・コンディション低下ペナルティ | 2503dd5 |
| ④ | 交流戦（gameDay 60〜94） | 対戦相手をリーグをまたいで選出・交流戦バッジ表示 | 2503dd5 |

---

## Tier 2（ゲームプレイ深化）— ✅ 完了

| # | 機能 | 内容 | 状態 |
|---|------|------|------|
| ⑤ | 球種・コース表示 | イベントログに球種バッジ・コースラベルを表示（ui.jsx / TacticalGame.jsx） | ✅ 完了 |
| ⑥ | 選手個人成績グラフ | CareerTable にキャリア推移 LineChart を追加・指標セレクター付き（Tabs.jsx） | ✅ 完了 |

---

## Tier 3（OOTP 核心 — シミュレーション信頼性）— ✅ 完了

> **OOTP が面白い理由**: 情報の不確実性。スカウトは完璧ではなく、選手の本当の能力は試合で初めて証明される。

| # | 機能 | 内容 | 実装コミット |
|---|------|------|-------------|
| ⑧ | スカウティング精度・隠し能力値 | `scoutedValue()` でノイズ付き能力値表示。スカウト地域の `regionFactor` と予算で精度変動。`?` マーク付き推定値表示 | 566fe17 |
| ⑨ | 選手モラル | 個人モラル(0〜100)が打撃・投球に影響。シーズン末に出場機会・年俸・勝率でモラル変動。`MoralBadge` でUI表示 | 566fe17 |
| ⑩ | NPB 固有ルール深化 | `serviceYears` トラッキング・FA資格（高卒8年/大卒7年・海外9年）・育成3年ルール・支配下登録変換・最低年俸(支配下420万/育成240万) | 566fe17, 6f1c470 |

---

## Tier 4（FM 深化 — 経営の手触り）— ✅ 完了

> **FM が面白い理由**: すべての選択に人間が絡む。選手・スタッフとの関係性が結果に波及する。

| # | 機能 | 内容 | 実装コミット |
|---|------|------|-------------|
| ⑪ | 試合内戦術詳細化 | エンドラン実装（単打→走者3塁・2塁打→走者生還・三振/アウト時CS判定）＋投球方針セレクタ（速球/変化球/コントロール重視）をTacticalGame.jsx に追加 | 1401bf9 |
| ⑫ | コーチングスタッフ管理 | シーズン中効果追加: 走塁コーチ→盗塁率UP / 投手コーチ→疲労耐性UP / 守備コーチ→怪我回復速度UP / メンタルコーチ→シーズン末モラル回復ボーナス | 1401bf9 |
| ⑬ | 長期フランチャイズ記録・球団史 | 優勝旗・ペナント履歴（seasonHistory.championships）/ 球団殿堂カード表示強化（通算成績付き）/ 殿堂入り通知メール | 1401bf9 |

---

## Tier 5（フランチャイズ完成）— ✅ 完了

> **フランチャイズ経営の深み**: 交渉の駆け引き・球場への長期投資・ドラフト戦略の三位一体で長期経営のリアリティを実現。

| # | 機能 | 内容 | 実装コミット |
|---|------|------|-------------|
| ⑭ | トレード交渉 UI 深化 | 最大3ラウンドのカウンター交渉。ラウンドが進むほど CPU の要求が厳しくなる。「修正して再提案」ボタン。CPU の交渉理由（本音）表示。ラウンドインジケーター | ee4c218 |
| ⑮ | 球団財政・球場投資 | 球場レベル Lv0〜3（チケット収入 1.0x / 1.25x / 1.6x / 2.0x）。投資コスト 500万/1000万/2000万。シーズン累計収益・収入予測の可視化カード | ee4c218 |
| ⑯ | ドラフト戦略深化 | 投手/野手スカウト予算配分スライダー（10〜90%）。配分に応じた自動事前開示（最大8名×種別）。scoutedValue によるノイズ付き能力値表示（高配分→低ノイズ）。Tier 3 ⑧ と連動 | ee4c218 |

---

## バグ修正 / インフラ改善（Tier 5 後）— ✅ 完了

| # | 種別 | 内容 | 実装コミット |
|---|------|------|------------|
| B1 | **[P0] CPUプレイヤー個人成績未更新** | CPU vs CPU ゲームで選手個人成績（打率・ERA 等）が集計されず、MVP・沢村賞・記録が破綻していたバグを修正。`runBatchGames` / `handleAutoSimEnd` / `handleTacticalGameEnd` の全パスで `applyGameStatsFromLog` / `applyPostGameCondition` / `tickInjuries` / `checkForInjuries` を両チームに適用 | 9b05b2a |
| B2 | **[P1] handleStartGame 二重カウント** | `handleStartGame` がモード選択前に CPU 試合をシムして状態を commit していた → ハブに戻って再トリガーで二重カウント。CPU シムをモード選択後（`handleAutoSimEnd` / `handleTacticalGameEnd`）に移動して修正 | 9b05b2a |
| B3 | **[P2] CS Final シード誤り** | `PlayoffScreen.simAllRemaining()` が `cs1_se.teams[0]`（2位通過チーム）を CS Final の1位チームとして扱っていた → `state.se1` / `state.pa1`（リーグ1位・アドバンテージあり）に修正 | 9b05b2a |
| S1 | **NPBリアル日程システム** | シーズン開幕時に143日分の全試合日程を事前生成。2025年は実NPBパラメータ準拠、2026年以降は自動算出。甲子園ブラックアウト（阪神→京セラ代替・オリックスはアウェイ強制）、交流戦18日程、Berger方式リーグ内125日程を実装 | 8d3b00b |
| S2 | **交流戦バグ修正（対戦相手重複）** | 旧実装で `paHostsMap` 適用後も `(ceIdx+r)%6` 回転でラウンド0と同一PAチームがラウンド2で再マッチした（ヤクルト vs 西武が6試合）を offset-based rotation で修正 → 全CEチームが各PAチームと正確に3試合ずつ対戦 | 8d3b00b |
| S3 | **オリックスホーム試合数改善** | 甲子園ブラックアウト制約でオリックスホームが57試合に低下（目標71〜72）。PAラウンドを並び替えてブラックアウト日付にオリックスアウェイラウンドを優先配置 → 69試合に改善（±3は Berger 固定チームの構造的制約） | 8d3b00b |
| B4 | **[P1] 日程タブが常に「読み込み中」表示** | `schedule` を `useEffect([year, teams.length])` のみで生成していたため、同年セーブのロード時（year・teams.length 不変）と新規チーム選択時にスケジュールが再生成されず `null` のまま ScheduleTab が「読み込み中…」固定になるバグを修正。`handleLoad` と `handleSelect` でそれぞれ `setSchedule(generateSeasonSchedule(...))` を明示呼び出しするよう変更 | e1b5273 |
| B5 | **[P1] 翌年開幕時に日程タブが旧年スケジュール表示** | `handleNextYear` が `setSchedule` を呼ばずに `setYear(y=>y+1)` + `setScreen("hub")` のみ発火させていたため、翌年ハブ画面の初期レンダリング時点でスケジュールが前年分のままになるバグを修正。`handleNextYear` に `setSchedule(generateSeasonSchedule(year+1, teams))` を追加 | — |
| B6 | **[P0] エントリーポイント誤設定による日程タブ完全未表示** | `src/main.jsx` の import が `'../combined-artifact'`（旧全結合ファイル）を参照しており `src/App.jsx` が一切使われていなかった。B4/B5 を含む全修正が未反映だった。import を `'./App'` に変更して修正 | — |
| B7 | **[P1] バッチシムで中継ぎが起用されず先発が常に完投** | `checkStopCondition()` のピッチカウント閾値条件が `!gs.isTop`（自チームが打席の回）を見ていたため、自チーム投手が投球中（`isTop=true`）に閾値に達しても交代条件が発火しなかった。`!gs.isTop` → `gs.isTop` に修正 | bce22ba |
| B8 | **[P0] 相手チーム投手が全試合で絶対に交代しない（本当の根本原因）** | `initGameState` に `opBullpen` が存在せず `quickSimGame` に相手投手の疲労チェックが一切なかった。全オートシム・バッチシム・CPU vs CPU で相手先発は常に完投していた。`opBullpen` フィールドを追加し `quickSimGame` に相手投手の自動交代ロジックを追加 | — |
| E1 | **スタミナ連動疲弊度計算** | `calcEffectiveFatigue(pitchCount, pitcher)` を新設。`effectiveStamina = stamina × (condition/100)` でコンディションも加味し疲弊度%で交代判断。`PITCH_WARNING/PITCH_LIMIT` を廃止し `FATIGUE_WARNING=83/FATIGUE_LIMIT=100/PITCH_HARD_CAP=130` に置換。スタミナ高の投手は長く投げられ、低コンディション時は早く疲弊するようになった | — |
| E2 | **継投パターン設定画面** | RosterTab に「📋 継投」サブタブを追加。①先発ローテーション並び替え・除外・追加、②抑え/セットアッパー指名（重複禁止バリデーション）、③中継ぎ優先順設定。`team.pitchingPattern` フィールドを新設し `pickBullpenArm` に反映。指名投手不在時はスコアベース選択にフォールバック | — |
| B9 | **[P0] 起動時の黒画面（useState TDZ エラー）** | `App.jsx` で `news` / `mailbox` / `cpuTradeOffers` / `draftPool` / `draftResult` / `playoff` / `draftAllocation` の7つの `useState` がコンポーネント中盤（旧 line 625–631）に後置されており、それより前の `tabBadges` useMemo 依存配列が `mailbox` を宣言前に参照し TDZ `ReferenceError` が発生。React レンダリングがクラッシュして黒画面になっていた。7つの宣言を `schedule` の直後（line 62 以降）に移動して修正 | 1394edd |

---

## Tier 7 先行実装（㉓ 歴史データベース 部分実装）

> Tier 7 着手前に GAP として実装した先行機能。㉓「歴史データベース」の一部に相当。

| # | 機能 | 内容 | 実装コミット |
|---|------|------|-------------|
| F2 | **standingsHistory 蓄積** | シーズン終了時に全チームの最終順位スナップショット（勝敗・得失点）を `seasonHistory.standingsHistory` に蓄積。RecordsTab に「年度別最終順位」セクションを追加（セ/パ各リーグ、新→旧順） | 8056f2c |
| F3 | **年度アーカイブ UI** | RecordsTab の「年度別最終順位」を `<details>/<summary>` で折りたたみ可能に（最新年のみ展開）。「歴代シーズン表彰」に MVP/沢村賞/新人王を全年度分・色分け表示 | c6a19f7 |
| F4 | **記録更新ニュース通知** | `updateRecords` 返り値を `{ records, broken }` に拡張。シーズン本塁打・打率・奪三振の記録更新時にニュースタブへ通知。初年度・通算記録（careerHR/careerW）は通知対象外 | f0594b0 |

---

## Tier 6（ポリッシュ）

| # | 機能 | 概要 | 状態 |
|---|------|------|------|
| ⑰ | カスタムチーム作成 | チーム名・色・本拠地を自由設定 | 未着手 |
| ⑱ | 統計データエクスポート | シーズン成績を CSV / JSON でダウンロード | 未着手 |
| ⑲ | モバイル最適化 | タッチ操作対応・スマートフォン向けレイアウト改善 | 未着手 |

---

---

## Tier 7（複数年フランチャイズ基盤）— ✅ 完了

> **OOTPの核心**: 単年ではなく「王朝を作る」体験。選手が成長し、衰え、引退していく時間軸がゲームの深みをすべて生む。

| # | 機能 | 概要 | 実装コミット |
|---|------|------|-------------|
| ⑳ | マルチシーズン継続 | `handleNextYear` 完成: 予算リセット（baseIncome + revenue×0.6 − payroll）/ `new_season` 画面遷移 / `NewSeasonScreen`（引退サマリー・ドラフト結果・ブレイクアウト表示）。careerLog をコンパクト25フィールド形式に変更（25年以上 localStorage 安全） | c1c404b |
| ㉑ | 選手加齢・引退パイプライン | `calcRetireWill` 精緻化（40歳:+50 / 38-39:+28 / 36-37:+12 / 35:+3 / シーズンエンド怪我:+25）。CPU チーム引退選手を `t.history[]` に記録（alumni 漏れ修正）。引退時 `exitYear`/`exitReason`/`tenure` 保存 | c1c404b |
| ㉒ | 能力自然変化（成長/衰退） | `PITCHER_DECLINE_WEIGHTS`（velocity:1.4 / stamina:1.2 / control:0.8 等）導入。28〜34歳の2%確率晩年ブレイクスルー。`peak→earlyDecline` 遷移時に `peakAbilities` スナップショット保存（衰退後も peak値60%を下限）。`GrowthSummaryScreen` に33歳以上の衰退警告追加 | c1c404b |
| ㉓ | 歴史データベース | RecordsTab を5サブタブ（今季表彰 / タイトル歴代 / 通算記録 / 年度別順位 / 殿堂）に再編。`calcTitles()` でセ/パ8タイトル計算。`standingsSnap` に `awards`（MVP/沢村賞/新人王/タイトル）を追加。セ/パ別リーグ MVP 対応 | c1c404b |

---

## Tier 8（ファームシステム拡張・育成戦略深化）— 未着手 🟡 高優先

> 「組織全体を経営する」感覚。一軍に影響するファーム育成の戦略性を高める。

| # | 機能 | 概要 | 状態 |
|---|------|------|------|
| ㉔ | 二軍簡易シミュレーション | 確率的シム（farmSimGame）で143試合をバッチ処理。二軍成績をstats2（6統計）に蓄積。CE→イースタン/PA→ウエスタン（各6球団）でリーグ別タイトル表彰。リアル選手データは不要（procedural生成） | 未着手 |
| ㉕ | 育成→支配下昇格とFA管理 | `entryType`（高卒/大卒/社会人/外国人）フィールドを新設しFA閾値を明示化。累積日数方式（daysOnActiveRoster/120日=1年）でFA権判定。外国人選手はFA取得（960日）で外国人枠から自動免除。育成→支配下→一軍昇格を1フロー化 | 未着手 |
| ㉖ | 怪我降格・登録抹消10日ルール・支配下70人枠 | 怪我>10日で自動二軍降格（cooldown付き）、≤10日で確認ダイアログ。farm選手にもtickInjuries適用。怪我回復/枠空き時に昇格サジェスト通知。MAX_SHIHAKA_TOTAL=70の総枠管理 | 未着手 |
| ㉗ | 選手育成目標設定 | 「一軍レギュラー狙い」「支配下昇格目標」「投手→野手転向」等のNPB色ある目標UI。目標に基づくtrainingFocus自動選択 | 未着手 |

---

## Tier 9（フロント・メディア・選手関係）— 未着手 🟡 高優先

> **FMの核心**: すべての選択に人間が絡む。関係性が結果に波及する。OOTP優先方針により中期実装。

| # | 機能 | 概要 | 状態 |
|---|------|------|------|
| ㉘ | フロント目標・信頼度 | 開幕前にオーナーから今季目標設定。達成度→信頼度(0-100)。信頼度が補強予算に影響 | 未着手 |
| ㉙ | 選手個別コミュニケーション | 「出場機会について話す」等の選択肢。会話結果でモラル±10〜20。不満選手の放出要求イベント | 未着手 |
| ㉚ | 記者会見（簡易版） | 月1〜2回の質問イベント。回答によりチームモラル・メディア評価変化 | 未着手 |
| ㉛ | ファン感情 | 観客動員数連動のファン感情スコア。連敗・不人気選手放出でチケット収入減。優勝でブースト | 未着手 |

---

## Tier 10（NPB固有システム深化）— 未着手 🟡 高優先

> NPB版OOTP/FMとしての差別化。日本プロ野球ならではのルールと文化を深く実装する。

| # | 機能 | 概要 | 状態 |
|---|------|------|------|
| ㉜ | ポスティングシステム | FA取得前のポスティング申請。MLB球団からの入札額シミュ。移籍金の球団収入化。選手のoverseas値が意思決定に影響 | 未着手 |
| ㉝ | ドラフト拡張（高校生/大学生/社会人） | 指名時に即戦力/素材型の区分表示。高校生: entryAge=18・長growth期。社会人: entryAge=24・即支配下 | 未着手 |
| ㉞ | オールスターゲーム | ゲームDay75付近でイベント。成績から自動選出＋監督3人追加。簡易試合シミュ | 未着手 |
| ㉟ | 戦力外通告・ウェーバー | 10/2以降の戦力外通告期間。ウェーバー優先権（下位球団優先）。未取得→FA移行 | 未着手 |
| ㊱ | 外国人選手獲得 | FA市場に毎年5〜10人の外国人選手。代理人交渉シミュ。外国人枠管理UI | 未着手 |

---

## Tier 11（シミュレーション精度向上・AI強化）— 未着手 🟢 中優先

> CPU球団をOOTPレベルの意思決定に近づける。リーグ全体のリアリティが向上する。

| # | 機能 | 概要 | 状態 |
|---|------|------|------|
| ㊲ | CPU球団の積極的補強 | CPU球団が毎オフにFA市場・トレードを積極活用。球団ニーズ分析（弱いポジション特定）→優先補強 | 未着手 |
| ㊳ | トレードデッドライン | ゲームDay100付近で活発化。優勝争いチーム→買い手。再建チーム→売り手。CPU自動トレード頻度増 | 未着手 |
| ㊴ | スプリングトレーニング | 2〜3月の紅白戦シミュ。成績によりスターター争い決定。能力±3の季節変動 | 未着手 |
| ㊵ | 怪我の詳細化 | 故障箇所別（肩・肘・膝・腰等）。同箇所再発リスク（+20%）。IL-10/IL-30相当の登録制度 | 未着手 |

---

## Tier 12（UI/UX・品質・拡張性）— 未着手 🟢 中優先

| # | 機能 | 概要 | 状態 |
|---|------|------|------|
| ⑰ | カスタムチーム作成 | チーム名・色・本拠地を自由設定（Tier 6 残件） | 未着手 |
| ⑱ | 統計データエクスポート | シーズン成績を CSV / JSON でダウンロード（Tier 6 残件） | 未着手 |
| ⑲ | モバイル最適化 | タッチ操作対応・スマートフォン向けレイアウト改善（Tier 6 残件） | 未着手 |
| ㊶ | 統計ビジュアライゼーション強化 | 複数年グラフ・レーダーチャート・対戦チーム別勝率ヒートマップ | 未着手 |
| ㊷ | ゲーム速度設定 | 試合スキップ速度（1x/2x/5x/瞬時）・週単位自動進行モード | 未着手 |
| ㊸ | コミッショナーモード | 全球団自由操作・選手能力直接編集・歴史的シナリオ | 未着手 |

---

## 保守性・コード品質（横断的課題）

> 新機能追加を安全・高速に行うための技術基盤整備。エラーが起きづらく、長期的に保守しやすいコードベースを目指す。

### P0: Tier 7着手前に必須

| # | 課題 | 内容 | 状態 |
|---|------|------|------|
| T1 | localStorage → IndexedDB 移行 | 複数年データで5MB上限を超過する前に移行。saveload.js を差し替え。セーブのバージョニング導入 | 🔶 延期（careerLog コンパクト形式で25年分安全確認済み。Tier 8 着手前に再評価） |
| T2 | React Error Boundary 追加 | シミュ中エラーで画面が白くなるのを防止。各主要コンポーネントをラップ。エラー時にリカバリー画面を表示 | ✅ 完了 (fb47d0f) |

### P1: **🔴 Tier 8 着手前に完了（現フォーカス）**

| # | 課題 | 内容 | 状態 |
|---|------|------|------|
| T3 | App.jsx 分割（923行） | useGameState.js / useSeasonFlow.js / useOffseason.js に切り出し。HubScreen / GameScreen 等を独立ファイル化 | ✅ 完了（hooks 3本抽出・App.jsx を 200行のrender専用に縮小） |
| T4 | Tabs.jsx 分割（1,282行） | タブごとに独立ファイル化（src/components/tabs/RosterTab.jsx 等）。ScheduleTab.jsx も tabs/ に移動し Tabs.jsx バレルに統合 | ✅ 完了 |
| T5 | セーブデータバリデーション | ロード時の必須フィールドnullチェック。破損時のバックアップ復元（ローリング2世代） | ✅ 完了 |
| T6 | JSDoc 型注釈 | player.js / simulation.js の主要関数から順次追加。TypeScript 移行の前段 | 未着手 |

### P2: 中長期

| # | 課題 | 内容 | 状態 |
|---|------|------|------|
| T7 | Vitest ユニットテスト | resolveAtBat / calcEffectiveFatigue / calcOfferScore / FA資格判定 等のエンジン関数から導入 | 🔶 部分実装（純粋関数32件 `src/engine/__tests__/`） |
| T8 | useReducer 移行 | 複数年でstate更新競合が増える前に。dispatch(action) パターンで状態遷移を明示化 | 未着手 |

---

## UI改善（横断的課題）

> OOTP/FMレベルの情報量を快適に扱えるUIへ。新機能追加と並行して既存画面も順次改善する。

### P0: 即着手

| # | 機能 | 概要 | 状態 |
|---|------|------|------|
| U1 | **グローバル選手詳細モーダル** | **どのタブ・どの画面からでも**選手名クリックで能力・成績・契約情報を表示。PlayerModal.jsx を新規作成し App.jsx ルートに配置。ESCキー・背景クリックで閉じる | ✅ 完了 (d7885d0) |
| U2 | ダッシュボード画面 | ハブのランディングページ。順位・次の試合・要対応アクション（赤/黄/グレーバッジ）・直近5試合結果を一覧表示。`DashboardTab.jsx` 新規作成。`recentResults` state で直近5試合を追跡 | ✅ 完了 (345c0f3) |
| U3 | 通知バッジシステム | FA期限・ロースター枠超過→赤。トレード提示・契約満了→黄。FA市場→グレー。`tabBadges` useMemo でタブに数値バッジ常時表示（U2 と同時実装） | ✅ 完了 (345c0f3) |

### P1: Tier 7と並行

| # | 機能 | 概要 | 状態 |
|---|------|------|------|
| U4 | ナビゲーション整理 | 13タブをカテゴリ分けナビに。頻出タブ（ロスター・試合・順位）はトップバー常時表示 | 未着手 |
| U5 | 選手能力グレード表示 | 数値を S/A/B/C/D/E グレードに変換（OOTP方式、S=90+/A=80+/B=65+/C=50+/D=35+/E<35）。PlayerModal.jsx の `abilityGrade()` + `AbilityBar` で色付きバッジ表示 | ✅ 完了 (c1c404b) |
| U6 | 選手比較ツール | 2〜4名を横並び比較。各能力値の差分をカラーハイライト（高い方が緑）。トレード・FA判断に活用 | 未着手 |

### P2: Tier 8以降

| # | 機能 | 概要 | 状態 |
|---|------|------|------|
| U7 | ラインナップD&D | @dnd-kit/core で打順をドラッグ&ドロップ変更 | 未着手 |
| U8 | バッチシム結果サマリー改善 | 試合ハイライトメッセージ・順位変動・負傷情報を5試合分まとめて表示 | 未着手 |
| U9 | ダーク/ライトモード | CSS変数ベースで切り替え。テーマ設定をlocalStorageに保存 | 未着手 |

---

## ボツ / 方向性から外した項目

| 機能 | 理由 |
|------|------|
| 実績/チャレンジシステム | アーケード的要素。OOTP/FM のゴールと方向性が異なるため削除。殿堂システム（⑬）で代替 |
| ⑦ 難易度設定 | CPU 難易度は対戦相手の能力差として自然に生じるため、別途設定する必要性が薄い。Tier 3 から削除 |
